// Barrel export for shared TypeScript types & enums (TrialClass, Booking,
// BookingStatus, PaymentStatus, etc). One file per domain entity:
// libs/shared/types/{entityName}.ts
//
// Populated per domain/domain-model.md.
export * from "./trialClass";
export * from "./booking";
export * from "./student";
